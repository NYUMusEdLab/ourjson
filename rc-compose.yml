app:
  build: git@github.com:NYUMusEdLab/ourjson.git
  restart: always
  environment:
    - MONGO_DB_NAME=ourjson
    - VIRTUAL_HOST=betapi.musedlab.org
  expose:
    - "8080"
  links:
    - mongo
mongo:
  restart: always
  image: mongo:3.2
